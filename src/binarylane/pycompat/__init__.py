"""Central repository of modules providing compatibility with newer Python releases via backported modules.

Each feature is access directly by module name (and not imported here) to minimise impact on CLI startup time.
"""
